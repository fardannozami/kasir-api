{
  "version": 2,
  "builds": [
    { "src": "api/index.go", "use": "@vercel/go" }
  ],
  "routes": [
    { "src": "/", "dest": "/api" },
    { "src": "/health", "dest": "/api" },
    { "src": "/swagger.json", "dest": "/api" },
    { "src": "/api/(.*)", "dest": "/api" }
  ]
}
